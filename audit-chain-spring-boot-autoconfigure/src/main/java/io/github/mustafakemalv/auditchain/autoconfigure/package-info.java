/**
 * Spring Boot auto-configuration: wires an {@code AuditChain}, chooses a store (JDBC when a
 * {@code DataSource} is present, otherwise in-memory), and binds {@code audit-chain.*} properties.
 */
package io.github.mustafakemalv.auditchain.autoconfigure;
