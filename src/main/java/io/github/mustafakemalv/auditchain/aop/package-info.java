/**
 * Declarative auditing: the {@code @Audited} annotation, the actor provider, and the aspect that
 * records a successful method invocation as an audit event.
 */
package io.github.mustafakemalv.auditchain.aop;
