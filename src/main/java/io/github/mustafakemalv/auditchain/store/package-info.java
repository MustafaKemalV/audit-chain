/**
 * Storage SPI and implementations: the append-only {@code AuditStore} contract, an in-memory store,
 * a JDBC store, and the reversible details codec.
 */
package io.github.mustafakemalv.auditchain.store;
