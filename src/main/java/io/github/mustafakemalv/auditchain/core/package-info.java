/**
 * Core domain and crypto: the audit record and event, the deterministic canonical encoding, the
 * HMAC primitives, the chain-node and checkpoint types, and the verification result.
 */
package io.github.mustafakemalv.auditchain.core;
